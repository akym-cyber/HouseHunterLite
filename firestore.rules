rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEBUG MODE: temporarily unblock mobile-web while schema compatibility is stabilized.
    // Re-tighten these rules after production flows are validated.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
